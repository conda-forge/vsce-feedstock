{% set name = "vsce" %}
{% set version = "2.6.6" %}

package:
  name: {{ name }}
  version: {{ version }}

source:
  url: https://registry.npmjs.org/{{ name }}/-/{{ name }}-{{ version }}.tgz
  sha256: 163cd970b21fb1d9a53dc269461a8bb4046b392f9e6cca9fd16efaca1d6c02d0

build:
  number: 0
  skip: true  # [win]

requirements:
  build:
    - sysroot_linux-64 2.17  # [linux64]
    - {{ compiler('c') }}    # [ppc64le]
    - {{ compiler('cxx') }}  # [ppc64le]
    - pkg-config             # [ppc64le]
    - make                   # [ppc64le]
  host:
    - nodejs
    - yarn
    - glib                   # [ppc64le]
    - libsecret              # [ppc64le]
  run:
    - nodejs

test:
  commands:
    - vsce --help

about:
  home: https://github.com/microsoft/vscode-vsce
  license: MIT
  license_file:
    - LICENSE
    - ThirdPartyLicenses.txt
  summary: VS Code Extension Manager

extra:
  recipe-maintainers:
    - xhochy
